name: create-lessons-mobile-repairing

on:
  schedule:
    - cron: '40 9 * * *'
  workflow_dispatch: {}

concurrency:
  group: create-lessons-mobile-repairing
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Repo checkout karo
        uses: actions/checkout@v4

      - name: Python setup karo
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Dependencies install karo
        run: |
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: ffmpeg install karo (AI video ke liye)
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Mobile Repairing Business ka lesson generate karo
        env:
          COURSE_SLUG: mobile-repairing
          MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}
          SITE_URL: ${{ secrets.SITE_URL }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          FB_PAGE_ACCESS_TOKEN: ${{ secrets.FB_PAGE_ACCESS_TOKEN }}
        run: python generate_post.py

      - name: Changes commit aur push karo
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --staged --quiet || git commit -m "Auto: Mobile Repairing Business — naya lesson [skip ci]"
          git pull --rebase origin main
          git push
