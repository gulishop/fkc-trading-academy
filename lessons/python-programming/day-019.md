# Day 19 — Working with APIs: Fetching and Using External Data

**Concept:**
APIs (Application Programming Interfaces) allow your Python programs to communicate with external services like weather apps, social media, or payment gateways. We'll learn how to make HTTP requests to APIs, handle responses (JSON/XML), and extract useful data using the `requests` library.

**Example:**
1. Install `requests` (if not installed): `pip install requests`
2. Fetch data from a free API (e.g., JSONPlaceholder for fake posts):
   ```python
   import requests
   response = requests.get("https://jsonplaceholder.typicode.com/posts/1")
   data = response.json()  # Convert JSON response to Python dict
   print(data["title"])    # Access specific data
   ```
3. Handle errors (e.g., invalid URL):
   ```python
   try:
       response = requests.get("https://invalid.url")
       response.raise_for_status()  # Raises error for bad status codes
   except requests.exceptions.RequestException as e:
       print("Error:", e)
   ```

**Practice:**
Use the [OpenWeatherMap API](https://openweathermap.org/api) (sign up for a free API key) to fetch and print the current temperature of your city. Hint: Use `requests.get()` with the API endpoint and parse the JSON response.

**Mini Project:**
Build a **5-day weather forecast CLI tool** that:
1. Takes a city name as input.
2. Fetches weather data from OpenWeatherMap API.
3. Prints the forecast in a readable format (e.g., "Day 1: Sunny, 25°C").

**Answer Key:**
API request, JSON parsing, error handling, HTTP status codes