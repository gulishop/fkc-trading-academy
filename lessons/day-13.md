# Day 13 — Functions

- **Concept:** Baar baar use hone wala code ek naam de dena
- **Example:**
  ```python
  def add(a, b):
      return a + b

  print(add(10, 5))
  ```
- **Practice:** `multiply()` aur `subtract()` functions banwayen
- **Mini Project:** "Function-based Calculator" — har operation ek function ho
